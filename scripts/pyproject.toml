[project]
name = "image-to-map"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.79.0",
    "httpx>=0.28.1",
    "pillow>=12.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
